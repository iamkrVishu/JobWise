{
  "name": "jobwise-fullstack",
  "version": "1.0.0",
  "description": "JobWise - Smart Job Application Tracker with Feedback System",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "start": "cd server && npm start",
    "install-deps": "npm install && cd client && npm install && cd ../server && npm install"
  },
  "keywords": ["job-tracker", "feedback-system", "react", "nodejs", "mongodb"],
  "author": "JobWise Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}